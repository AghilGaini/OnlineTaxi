@model Database.Domain.ViewModels.Color.ColorsViewModel
@{
    ViewBag.Title = "مدیریت رنگ ها";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h3>لیست رنگ ها</h3>

<table class="table table-dark">
    <thead>
        <tr>
            <th>نام ماشین</th>
            <th>کد رنگ</th>
            <th>عملیات</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.ColorsInfo)
        {
            <tr>
                <td>
                    @item.Name
                    <input asp-for="@item.Id" type="hidden">
                </td>
                <td>
                    @item.Code
                </td>
                <td>
                    <button type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown"
                        aria-haspopup="true" aria-expanded="false">
                        عملیات
                    </button>
                    <div class="dropdown-menu shadow animated--grow-in">
                        @foreach (var link in Model.Actions)
                        {
                            <a style='margin-right:16px' class="dropdown-item"
                       asp-controller="@link.Controller" asp-action="@link.Action" asp-route-Id="@item.Id">
                                @link.Title
                            </a>
                        }
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
<a asp-action="Create" asp-controller="Color" class="btn btn-success">
    جدید
</a>



@model Database.Domain.ViewModels.Role.RoleViewModel
@{
    ViewBag.Title = "مدیریت نقش";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h3>لیست نقش ها</h3>

<table class="table table-dark">
    <thead>
        <tr>
            <th>نام</th>
            <th>نام سیستمی</th>
            <th>عملیات</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.RolesInfo)
        {
            <tr>
                <td>
                    @item.Title
                    <input asp-for="@item.Id" type="hidden">
                </td>
                <td>
                    @item.Name
                </td>
                <td>
                    <button type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown"
                        aria-haspopup="true" aria-expanded="false">
                        عملیات
                    </button>
                    <div class="dropdown-menu shadow animated--grow-in">
                        @foreach (var link in Model.Actions)
                        {
                            <a style='margin-right:16px' class="dropdown-item"
                       asp-controller="@link.Controller" asp-action="@link.Action" asp-route-Id="@item.Id">
                                @link.Title
                            </a>
                        }
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
<a asp-action="Create" asp-controller="Role" class="btn btn-success">
    جدید
</a>


